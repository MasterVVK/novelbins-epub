# Novel Translator & EPUB Generator

**Автоматизированная платформа для перевода китайских веб-новелл с AI и генерации EPUB**

Веб-приложение на Flask для профессионального перевода китайских веб-новелл (преимущественно жанр сянся/фэнтези) с использованием современных AI моделей. Система включает интеллектуальный парсинг с различных источников, многоэтапную редактуру с учетом контекста, терминологический глоссарий и генерацию качественных EPUB файлов.

---

## 🌟 Основные возможности

### 📚 Парсинг и импорт
- **Поддержка множества источников**: czbooks.net, qidian.com, EPUB файлы
- **🤖 Автоматическое решение Cloudflare Turnstile**:
  - Vision AI (Qwen3-VL) через Ollama - **$0 стоимость, локально**
  - Точность: 0px на тестах, ±5px на реальных скриншотах
  - Success rate: 70-85% (протестировано в production)
  - Fallback на VNC ручное решение при неудаче
- **Обход детекции**: undetected-chromedriver + реалистичные mouse events
- **Умная фильтрация**: Автоматическое удаление навигационных элементов и объявлений
- **Восстановление ошибок**: Двухпроходная система с повторными попытками при таймаутах
- **SOCKS5 прокси**: Поддержка прокси для обхода блокировок

### 🤖 AI перевод и редактура
- **Множество AI провайдеров**: Google Gemini, OpenAI GPT, Anthropic Claude, Ollama (локальные модели)
- **Ротация API ключей**: Автоматическое переключение между ключами при rate limiting
- **Контекстный перевод**: Использование контекста из предыдущих глав
- **Многоэтапная редактура**:
  - Анализ с оригинальным текстом
  - Исправление несоответствий
  - Улучшение стиля
  - Финальная полировка
- **Терминологический глоссарий**: Консистентность терминов (имена, техники, локации)
- **Параллельная обработка**: Многопоточный перевод и редактура (настраиваемое количество потоков)

### 📖 Генерация EPUB
- **Профессиональное качество**: Корректная структура с метаданными
- **Двуязычный режим**: Оригинальный китайский текст + русский перевод рядом в одном EPUB
- **Настраиваемые шаблоны**: Стили, обложки, форматирование
- **Многоязычная поддержка**: Правильная обработка китайских и русских символов
- **Гибкие опции**: Только перевод, только оригинал, или оба языка

### 🎛️ Управление и мониторинг
- **Веб-интерфейс**: Современный UI на Bootstrap 5 с real-time обновлениями
- **Live прогресс**: WebSocket для отображения процесса парсинга/перевода
- **VNC веб-доступ**: Встроенный noVNC для мониторинга браузера Selenium
- **Детальные логи**: Контекстное логирование всех операций
- **Управление задачами**: Запуск, остановка, отмена фоновых задач

---

## 🏗️ Архитектура

### Компоненты системы

```
┌─────────────────────────────────────────────────────────┐
│                    Flask Web Application                │
│  ┌─────────┐  ┌─────────┐  ┌─────────┐  ┌──────────┐  │
│  │  Views  │  │   API   │  │ WebSockets │  │  Auth  │  │
│  └────┬────┘  └────┬────┘  └─────┬────┘  └────┬─────┘  │
└───────┼───────────┼─────────────┼──────────────┼────────┘
        │           │             │              │
┌───────┼───────────┼─────────────┼──────────────┼────────┐
│       │           │             │              │        │
│  ┌────▼────┐ ┌────▼────┐   ┌────▼────┐   ┌────▼─────┐ │
│  │ Services │ │ Models  │   │ Celery  │   │Templates │ │
│  ├─────────┤ ├─────────┤   │ Tasks   │   └──────────┘ │
│  │ Parser  │ │ Novel   │   ├─────────┤                 │
│  │ Trans   │ │ Chapter │   │ Parsing │                 │
│  │ Editor  │ │ AI Model│   │ Trans   │                 │
│  │ Glossary│ │ Glossary│   │ Editing │                 │
│  │ EPUB    │ │ User    │   └────┬────┘                 │
│  └────┬────┘ └────┬────┘        │                      │
└───────┼───────────┼─────────────┼──────────────────────┘
        │           │             │
┌───────▼───────────▼─────────────▼──────────────────────┐
│                    SQLite Database                      │
│  novels │ chapters │ translations │ glossary │ tasks   │
└─────────────────────────────────────────────────────────┘

External Services:
┌─────────────┐  ┌──────────────┐  ┌────────────────┐
│   Redis     │  │  Selenium +  │  │  AI Providers  │
│   (Celery)  │  │  Chrome VNC  │  │ Gemini/GPT/... │
└─────────────┘  └──────────────┘  └────────────────┘
```

### Модульная структура Flask

**Web Application** (`web_app/app/`)
```
app/
├── __init__.py          # Factory pattern, Flask app creation
├── views.py             # Main UI routes
├── auth.py              # Authentication
├── celery_tasks.py      # Background job definitions
├── api/                 # REST API endpoints
│   ├── novels.py        # Novel CRUD operations
│   ├── chapters.py      # Chapter management
│   ├── parsing.py       # Parsing control
│   ├── editing.py       # Editing control
│   ├── glossary.py      # Glossary management
│   └── celery_monitor.py # Task monitoring
├── models/              # SQLAlchemy ORM models
│   ├── novel.py
│   ├── chapter.py
│   ├── ai_model.py
│   ├── glossary.py
│   ├── prompt_template.py
│   └── translation.py
├── services/            # Business logic layer
│   ├── universal_llm_translator.py      # Multi-provider AI translator
│   ├── translator_service.py            # Translation orchestration
│   ├── original_aware_editor_service.py # Main editing service
│   ├── glossary_service.py              # Glossary management
│   ├── parser_service.py                # Parser integration
│   ├── epub_service.py                  # EPUB generation
│   └── ai_adapter_service.py            # AI provider adapter
└── templates/           # Jinja2 HTML templates
```

**Parser System** (`parsers/`)
```
parsers/
├── parser_factory.py    # Factory pattern for parser creation
├── base/
│   └── base_parser.py   # Abstract base parser class
└── sources/
    ├── czbooks_parser.py   # czbooks.net (Selenium + VNC)
    ├── qidian_parser.py    # qidian.com
    └── epub_parser.py      # EPUB import
```

---

## 🛠️ Технологический стек

### Backend
- **Flask 3.1.1** - Web framework
- **SQLAlchemy + Flask-Migrate** - ORM и миграции БД
- **Celery 5.5.3** - Асинхронные задачи
- **Redis** - Message broker для Celery
- **Flask-SocketIO** - Real-time updates
- **Flask-Login** - Authentication

### Парсинг
- **Selenium 4.33** - Браузерная автоматизация
- **undetected-chromedriver** - Обход Cloudflare detection
- **BeautifulSoup4** - HTML парсинг
- **x11vnc + noVNC** - VNC server для удаленного доступа к браузеру

### AI интеграция
- **Google Generative AI 0.8.5** - Google Gemini
- **OpenAI API** - GPT модели
- **Anthropic API** - Claude модели
- **Ollama** - Локальные LLM модели
- **Qwen3-VL** - Vision AI для автоматического решения Cloudflare (через Ollama)

### Frontend
- **Bootstrap 5** - UI framework
- **Socket.IO client** - Real-time updates
- **noVNC** - Браузерный VNC viewer

### EPUB
- **EbookLib** - EPUB generation
- **Pillow** - Image processing

---

## 📥 Установка

### Системные требования
- **OS**: Linux (Ubuntu 20.04+), macOS, Windows (с WSL)
- **Python**: 3.10+
- **Redis**: 6.0+
- **Chrome/Chromium**: Для Selenium
- **RAM**: Минимум 8GB (рекомендуется 16GB+ для vision модели)
- **GPU** (опционально): NVIDIA с 8GB+ VRAM для Qwen3-VL (RTX 3090 идеально)
  - Без GPU: Cloudflare solver будет медленнее (~30 сек вместо ~5 сек)
- **Дисковое пространство**: ~100MB базовая установка + ~7GB для qwen3-vl:8b + ~5GB на 1000 глав
- **Ollama** (для автоматического решения Cloudflare): Локальный inference server

### Быстрый старт

```bash
# 1. Клонирование репозитория
git clone https://github.com/yourusername/novelbins-epub.git
cd novelbins-epub

# 2. Создание виртуального окружения
python3 -m venv .venv
source .venv/bin/activate  # Linux/macOS
# или
.venv\Scripts\activate     # Windows

# 3. Установка зависимостей
pip install -r requirements.txt

# 4. Установка системных зависимостей (Linux)
sudo apt-get update
sudo apt-get install -y \
    redis-server \
    chromium-browser \
    chromium-chromedriver \
    xvfb \
    x11vnc \
    websockify

# 5. Установка Ollama и Qwen3-VL (для автоматического решения Cloudflare)
# Установка Ollama
curl -fsSL https://ollama.com/install.sh | sh

# Скачивание vision модели (6GB)
ollama pull qwen3-vl:8b

# Проверка установки
ollama list | grep qwen3-vl

# Опционально: Более мощная модель для лучшей точности (21GB, требует 24GB VRAM)
# ollama pull qwen3-vl:32b

# 6. Настройка конфигурации
cp .env.example .env
# Отредактируйте .env (см. раздел Конфигурация)

# 7. Инициализация базы данных
python run_web.py init-db
# или
cd web_app && flask db upgrade

# 8. Запуск приложения
# Terminal 1: Flask веб-сервер
python run_web.py

# Terminal 2: Celery worker (для фоновых задач)
cd web_app
./start_celery_worker.sh
```

Приложение доступно по адресу: **http://localhost:5001**

VNC веб-доступ (браузер парсера): **http://localhost:6080/vnc.html**

---

## ⚙️ Конфигурация

### Переменные окружения (.env)

```bash
# Flask
FLASK_ENV=development
FLASK_APP=run_web.py
SECRET_KEY=your-secret-key-here

# База данных
DATABASE_URL=sqlite:///instance/novel_translator.db

# Celery
CELERY_BROKER_URL=redis://localhost:6379/1
CELERY_RESULT_BACKEND=redis://localhost:6379/1

# AI API ключи (разделенные запятой для ротации)
GEMINI_API_KEYS=your-gemini-key-1,your-gemini-key-2,your-gemini-key-3
OPENAI_API_KEY=your-openai-key
ANTHROPIC_API_KEY=your-anthropic-key

# Ollama (локальные модели)
OLLAMA_BASE_URL=http://localhost:11434

# Cloudflare Solver (автоматическое решение через Qwen3-VL)
CLOUDFLARE_SOLVER_ENABLED=true
CLOUDFLARE_SOLVER_MODEL=qwen3-vl:8b  # или qwen3-vl:32b для лучшей точности
CLOUDFLARE_SOLVER_MAX_ATTEMPTS=3

# Парсинг
PROXY_URL=socks5://user:pass@host:port  # Опционально
CLOUDFLARE_MAX_ATTEMPTS=5
PARSING_TIMEOUT=120

# VNC
VNC_DISPLAY=:99
VNC_RESOLUTION=1920x1080x24
VNC_PORT=5900
NOVNC_PORT=6080

# Логирование
LOG_LEVEL=INFO
LOG_FILE=logs/app.log
LOG_MAX_SIZE=10485760  # 10MB
LOG_BACKUP_COUNT=5
```

### Конфигурация Redis

```bash
# Запуск Redis
sudo systemctl start redis-server
sudo systemctl enable redis-server

# Проверка статуса
redis-cli ping
# Должно вернуть: PONG
```

### Настройка AI моделей

Модели настраиваются через веб-интерфейс: **Settings → AI Models**

Пример конфигурации модели:
```json
{
  "name": "Gemini 2.0 Flash",
  "provider": "gemini",
  "model_id": "gemini-2.0-flash-exp",
  "max_output_tokens": 8192,
  "temperature": 0.7,
  "is_active": true
}
```

---

## 🚀 Использование

### 1. Добавление новеллы

**Через веб-интерфейс:**
1. Перейдите на главную страницу
2. Нажмите "Добавить новеллу"
3. Введите URL источника (например, `https://czbooks.net/n/s6pcc1`)
4. Или загрузите EPUB файл
5. Нажмите "Создать"

**Через API:**
```bash
curl -X POST http://localhost:5001/api/novels \
  -H "Content-Type: application/json" \
  -d '{
    "title": "My Novel",
    "source_url": "https://czbooks.net/n/s6pcc1",
    "source": "czbooks"
  }'
```

### 2. Парсинг глав

**Автоматический парсинг:**
1. Откройте страницу новеллы
2. Нажмите "Начать парсинг"
3. **Cloudflare Turnstile решается автоматически** (через Qwen3-VL vision AI)
4. Если автоматика не сработала → VNC viewer развернется для ручного решения
5. Парсинг продолжится автоматически

**Особенности парсинга czbooks.net:**
- **🤖 Автоматическое решение Cloudflare**: Vision AI пытается пройти challenge (3 попытки, success rate ~70-85%)
- **Fallback на VNC**: При неудаче автоматически открывается VNC viewer для ручного решения
- **VNC мониторинг**: Доступен на http://localhost:6080/vnc.html
- **Cookies сохраняются** для последующих запусков
- **Двухпроходная система**: Первый проход + повторная попытка для failed глав
- **Реалистичные mouse events**: Для обхода детекции автоматизации

### 3. Перевод

**Настройка:**
1. Выберите AI модель: **Settings → AI Models**
2. Настройте параметры перевода:
   - Temperature (0.3-1.0)
   - Max tokens
   - Context size (сколько предыдущих глав использовать)

**Запуск перевода:**
1. Перейдите в новеллу → вкладка "Главы"
2. Выберите главы для перевода
3. Нажмите "Перевести выбранные"
4. Прогресс отображается в real-time

**Параллельный перевод:**
- Настройка: `novel.config['translation_threads']`
- Рекомендуется: 2-5 потоков (зависит от API limits)

### 4. Редактура

**Многоэтапная редактура с оригиналом:**
1. Убедитесь что главы переведены
2. Выберите главы → "Отредактировать"
3. Система автоматически:
   - Сравнит перевод с оригиналом
   - Исправит несоответствия
   - Улучшит стиль
   - Применит глоссарий

**Настройка потоков:**
```python
# В web_app/app/models/novel.py
novel.config['editing_threads'] = 3  # 1-10, рекомендуется 2-5
```

### 5. Глоссарий

**Автоматическое извлечение:**
1. Новелла → "Глоссарий" → "Извлечь термины"
2. AI автоматически найдет:
   - Имена персонажей
   - Техники и способности
   - Локации
   - Артефакты

**Ручное добавление:**
```python
POST /api/glossary/items
{
  "novel_id": 1,
  "term_chinese": "修真",
  "term_russian": "культивация",
  "category": "technique",
  "priority": 1
}
```

**Приоритеты:**
- 1 = Высокий (имена, ключевые термины)
- 2 = Средний (техники, локации)
- 3 = Низкий (общие термины)

### 6. Генерация EPUB

1. Новелла → "Скачать EPUB"
2. Опции:
   - **Двуязычный режим**: Оригинал + перевод рядом (идеально для изучения языка)
   - **Только перевод**: Чистый русский текст
   - **Только оригинал**: Чистый китайский текст
   - Включить только отредактированные главы
   - Пользовательская обложка
   - Настройки форматирования

**Формат двуязычного EPUB:**
```
Глава 1: Название

[Оригинал - 中文]
修真者站在山峰之上，俯瞰整個世界...

[Перевод - Русский]
Культиватор стоял на вершине горы, глядя на весь мир...

---

Глава 2: ...
```

**Через API:**
```bash
# Двуязычный EPUB
curl http://localhost:5001/api/novels/1/epub?mode=bilingual \
  --output my-novel-bilingual.epub

# Только перевод
curl http://localhost:5001/api/novels/1/epub?mode=translation \
  --output my-novel-ru.epub

# Только оригинал
curl http://localhost:5001/api/novels/1/epub?mode=original \
  --output my-novel-zh.epub
```

---

## 🔌 API Endpoints

### Novels
```
GET    /api/novels           # Список всех новелл
POST   /api/novels           # Создать новеллу
GET    /api/novels/:id       # Детали новеллы
PUT    /api/novels/:id       # Обновить новеллу
DELETE /api/novels/:id       # Удалить новеллу
GET    /api/novels/:id/epub  # Скачать EPUB
```

### Chapters
```
GET    /api/chapters/:id                # Детали главы
PUT    /api/chapters/:id                # Обновить главу
DELETE /api/chapters/:id                # Удалить главу
POST   /api/novels/:id/chapters/translate   # Перевести главы
GET    /api/novels/:id/chapters/export      # Экспорт в разные форматы
```

### Parsing
```
POST   /api/novels/:id/parse           # Запустить парсинг
POST   /api/novels/:id/parse/cancel    # Отменить парсинг
GET    /api/parsing/status/:task_id    # Статус задачи парсинга
```

### Editing
```
POST   /api/novels/:id/edit            # Запустить редактуру
POST   /api/novels/:id/edit/cancel     # Отменить редактуру
GET    /api/editing/status/:task_id    # Статус редактуры
```

### Glossary
```
GET    /api/glossary/:novel_id         # Получить глоссарий
POST   /api/glossary/items             # Добавить термин
PUT    /api/glossary/items/:id         # Обновить термин
DELETE /api/glossary/items/:id         # Удалить термин
POST   /api/glossary/extract/:novel_id # Автоизвлечение терминов
POST   /api/glossary/optimize/:novel_id # Оптимизация глоссария
```

### Celery Monitoring
```
GET    /api/celery/active              # Активные задачи
GET    /api/celery/stats               # Статистика worker'а
POST   /api/celery/tasks/:id/revoke    # Отменить задачу
```

### Logs
```
GET    /api/logs?novel_id=:id          # Логи новеллы
GET    /api/logs?chapter_id=:id        # Логи главы
```

---

## 🔧 Расширение функционала

### Добавление нового парсера

1. Создайте класс в `parsers/sources/your_parser.py`:

```python
from parsers.base.base_parser import BaseParser

class YourSiteParser(BaseParser):
    def __init__(self, auth_cookies=None, socks_proxy=None):
        super().__init__(auth_cookies, socks_proxy)
        self.base_url = "https://yoursite.com"

    def get_chapter_list(self, book_url: str) -> List[Dict]:
        """Получить список глав"""
        # Ваша реализация
        pass

    def get_chapter_content(self, chapter_url: str) -> Dict:
        """Получить содержимое главы"""
        # Ваша реализация
        pass
```

2. Зарегистрируйте в `parsers/parser_factory.py`:

```python
from parsers.sources.your_parser import YourSiteParser

ParserFactory.register_parser(
    'yoursite',
    YourSiteParser,
    ['yoursite.com']
)
```

### Добавление нового AI провайдера

1. Обновите `services/universal_llm_translator.py`:

```python
def translate_with_your_provider(self, text, **kwargs):
    """Перевод через ваш провайдер"""
    # Ваша реализация
    pass
```

2. Добавьте конфигурацию в `models/ai_model.py`:

```python
SUPPORTED_PROVIDERS = {
    'your_provider': {
        'name': 'Your Provider',
        'models': ['model-1', 'model-2']
    }
}
```

### Кастомизация промптов

Промпты редактируются через веб-интерфейс: **Settings → Prompt Templates**

Или через БД:
```python
from app.models import PromptTemplate

template = PromptTemplate.query.filter_by(
    name='translation_prompt'
).first()
template.template = "Your custom prompt: {text}"
db.session.commit()
```

---

## 🐛 Troubleshooting

### Celery worker не запускается

```bash
# Проверьте Redis
redis-cli ping

# Проверьте порты
netstat -tulpn | grep redis

# Перезапустите worker
cd web_app
./stop_celery_worker.sh
./start_celery_worker.sh

# Проверьте логи
tail -f /tmp/celery_worker_novelbins.log
```

### Cloudflare блокирует парсинг

**Автоматическое решение:**
```bash
# 1. Проверьте что Ollama запущен
curl http://localhost:11434/api/tags

# 2. Проверьте наличие vision модели
ollama list | grep qwen3-vl

# 3. Проверьте настройки в .env
grep CLOUDFLARE_SOLVER .env
# Должно быть: CLOUDFLARE_SOLVER_ENABLED=true

# 4. Проверьте логи автоматического решения
tail -f logs/app.log | grep -E "Qwen3-VL|Turnstile|iframe|Координаты"

# 5. Анализ debug файлов (если автоматика не работает)
cat /tmp/ollama_debug_attempt_1.json | jq '.response'
cat /tmp/ollama_debug_attempt_1.json | jq '.thinking'
cat /tmp/ollama_debug_attempt_1.json | jq '.done_reason'

# 6. Просмотр скриншотов
ls -lh /tmp/cloudflare_turnstile_attempt_*.png
```

**Ручное решение (fallback):**
```bash
# 1. Убедитесь что VNC запущен
ps aux | grep vnc

# 2. Откройте VNC viewer
# Браузер: http://localhost:6080/vnc.html

# 3. Пройдите Cloudflare проверку вручную

# 4. Cookies сохранятся автоматически
```

**Тестирование Cloudflare solver:**
```bash
# Запуск тестов на реальных скриншотах
python3 test_real_cloudflare_screenshots.py --model qwen3-vl:8b

# Базовые тесты vision API
python3 test_qwen_vision.py --model qwen3-vl:8b
```

### Ollama модели не работают

```bash
# Проверьте что Ollama запущен
curl http://localhost:11434/api/tags

# Проверьте наличие vision модели
ollama list | grep qwen3-vl

# Установите модель если отсутствует
ollama pull qwen3-vl:8b

# Для перевода: установите LLM модель
ollama pull mistral
ollama pull qwen3:32b

# Проверьте настройки в .env
echo $OLLAMA_BASE_URL
grep CLOUDFLARE_SOLVER_MODEL .env

# Мониторинг GPU (если используется)
watch -n 1 nvidia-smi
```

### База данных заблокирована

```bash
# SQLite не поддерживает множественные записи
# Убедитесь что только один worker запущен
ps aux | grep celery

# Или используйте PostgreSQL для production
```

---

## 📊 Производительность

### Рекомендуемые настройки

**Парсинг:**
- Concurrency: 1 (из-за Selenium)
- Timeout: 120-200 секунд на главу
- Cloudflare attempts: 5-10

**Перевод:**
- Threads: 2-5 (зависит от API rate limits)
- Context: 2-5 предыдущих глав
- Batch size: 1-3 главы

**Редактура:**
- Threads: 2-5 (оптимально 3)
- Используйте `OriginalAwareEditorService` для лучшего качества

### Оценка времени

Для новеллы из 1000 глав:
- **Парсинг**: ~5-8 часов (с учетом Cloudflare)
- **Перевод**: ~10-20 часов (зависит от модели и API limits)
- **Редактура**: ~15-30 часов (3 потока, 4 этапа)
- **Total**: ~30-58 часов

---

## 🤝 Contributing

Contributions are welcome! Please:

1. Fork the repository
2. Create a feature branch (`git checkout -b feature/amazing-feature`)
3. Commit your changes (`git commit -m 'Add amazing feature'`)
4. Push to the branch (`git push origin feature/amazing-feature`)
5. Open a Pull Request

### Development Guidelines

- Следуйте PEP 8 для Python кода
- Добавляйте docstrings ко всем публичным методам
- Пишите unit tests для новых фич
- Обновляйте документацию

---

## 📄 License

This project is licensed under the MIT License - see the [LICENSE](LICENSE) file for details.

---

## 🙏 Acknowledgments

- **Google Gemini** - Высококачественный AI перевод
- **Qwen Team (Alibaba)** - Отличная vision модель qwen3-vl для автоматического решения Cloudflare
- **Ollama** - Локальный inference сервер для бесплатного использования AI моделей
- **noVNC** - Браузерный VNC viewer
- **undetected-chromedriver** - Обход Cloudflare detection
- **Flask** - Мощный и гибкий веб-фреймворк
- **Celery** - Надежная система фоновых задач

---

## 📚 Дополнительная документация

### Cloudflare Turnstile Solver

Детальная документация по автоматическому решению Cloudflare Turnstile через vision AI:

- **[CLOUDFLARE_SOLVER_README.md](CLOUDFLARE_SOLVER_README.md)** - Полная документация по Cloudflare solver
  - Архитектура и принцип работы
  - Применённые оптимизации (2025-11-07)
  - Результаты тестирования (100% на тестовых данных)
  - Производительность и метрики
  - Troubleshooting и отладка

- **[ТЕСТИРОВАНИЕ_РЕЗУЛЬТАТЫ.md](ТЕСТИРОВАНИЕ_РЕЗУЛЬТАТЫ.md)** - Детальные результаты тестов
  - Сравнение "до и после" исправлений
  - Анализ координат и точности
  - Метрики улучшения (+200% успешность)

- **[ПАРСИНГ_ПРОБЛЕМА_АНАЛИЗ.md](ПАРСИНГ_ПРОБЛЕМА_АНАЛИЗ.md)** - Анализ проблем и решений
  - Корневые причины отказов
  - Детальный анализ логов
  - Технические рекомендации

---

## 📞 Support

- **Issues**: [GitHub Issues](https://github.com/yourusername/novelbins-epub/issues)
- **Discussions**: [GitHub Discussions](https://github.com/yourusername/novelbins-epub/discussions)
- **Documentation**: [Wiki](https://github.com/yourusername/novelbins-epub/wiki)
- **Cloudflare Solver**: См. [CLOUDFLARE_SOLVER_README.md](CLOUDFLARE_SOLVER_README.md)

---

## 🗺️ Roadmap

**Завершено:**
- [x] ✅ Автоматическое решение Cloudflare Turnstile через vision AI (Qwen3-VL)
- [x] ✅ Поддержка PostgreSQL для production

**В планах:**
- [ ] Улучшение Cloudflare solver до 90%+ success rate (тестирование qwen3-vl:32b)
- [ ] Docker контейнеризация
- [ ] Автоматическое определение терминов через Named Entity Recognition
- [ ] Интеграция с Telegram ботом для уведомлений
- [ ] Многоязычный UI (EN/RU/CN)
- [ ] Система рейтингов качества перевода
- [ ] Дашборд метрик Cloudflare solver (success rate, timing, координаты)
- [ ] Collaborative editing (несколько пользователей)
- [ ] API для внешних интеграций
- [ ] Мобильное приложение

---

**Made with ❤️ for web novel fans**
