# Анализ точности Qwen3-VL для определения координат Cloudflare Turnstile

## Резюме исследования

Проведено комплексное тестирование точности определения координат чекбокса Cloudflare Turnstile с помощью vision модели Qwen3-VL (qwen3-vl:8b).

## Ключевые находки

### 1. Детерминизм модели
✅ **100% стабильные результаты при temperature=0.0**
- 100 запросов на одном скриншоте → 100 одинаковых результата
- Стандартное отклонение: 0.00px
- Модель полностью детерминирована

### 2. Систематическое смещение
⚠️ **Смещение зависит от разрешения экрана**

#### На реальных разрешениях браузера:
| Разрешение | Ожидаемые координаты | Найдено моделью | Смещение (X, Y) |
|------------|---------------------|-----------------|-----------------|
| 800×600    | (170, 175)          | (285, 285)      | +115px, +110px  |
| 1280×720   | (273, 210)          | (285, 290)      | +12px, +80px    |
| 1920×1080  | (410, 315)          | (280, 290)      | -130px, -25px   |

**Стандартное отклонение на реальных разрешениях: σ_x = 166.8px**

#### На программном resize (от 1919×992):
| Scale | Ожидаемые | Найдено | Смещение на оригинале |
|-------|-----------|---------|----------------------|
| 60%   | (246, 174)| (285, 285)| +65px, +185px     |
| 70%   | (287, 203)| (285, 285)| -3px, +117px      |
| 80%   | (328, 232)| (282, 291)| -58px, +74px      |
| 90%   | (369, 261)| (283, 290)| -96px, +32px      |

**Стандартное отклонение на resize: σ_x = 60.4px**

### 3. Оптимизация промпта
❌ **Минимальное влияние (< 10px)**

Протестированы различные варианты:
- Указание размера изображения
- Упоминание текста "Verify you are human"
- Детальное описание чекбокса
- Различные форматы инструкций

**Результат**: Промпт-инженеринг не решает проблему смещения.

### 4. Grid подход
⚠️ **Работает только на больших разрешениях**

Нанесение координатной сетки 50px:

| Разрешение | Без Grid | С Grid 50px | Улучшение |
|------------|----------|-------------|-----------|
| 800×600    | 159.1px  | 190.4px     | **-19.6%** ❌ |
| 1280×720   | 80.9px   | 86.8px      | **-7.3%** ❌  |
| 1920×1080  | 132.4px  | 73.2px      | **+44.7%** ✅ |

**Среднее улучшение: +5.9%**

На исходном скриншоте 1919×992:
- Без grid: 130.0px ошибка
- С grid 50px: 60.8px ошибка (**+53% улучшение**)

### 5. Обрезка изображения
✅ **Отлично работает на вручную обрезанном**
- Вручную обрезанный mock: **7-8px ошибка**
- Автоматическая обрезка: 92-105px ошибка (сложно автоматизировать)

## Выводы

### Проблемы текущего подхода:
1. **Зависимость от разрешения**: На малых разрешениях модель ошибается на +274px, на больших на -130px
2. **Grid не универсален**: Помогает только на ≥1920px разрешениях
3. **Невозможность фиксированной коррекции**: σ = 166.8px на реальных разрешениях

### Почему это происходит:
- Cloudflare Turnstile widget рендерится по-разному на разных разрешениях
- Размер и позиция элементов меняются
- Модель видит контекст вокруг чекбокса, который варьируется

## Рекомендации

### Вариант 1: Фиксированное разрешение (РЕКОМЕНДУЕТСЯ) ✅

**Подход**: Всегда захватывать скриншот на разрешении 1920×1080

**Реализация**:
```python
# В czbooks_parser.py
driver.set_window_size(1920, 1080)
screenshot_path = driver.save_screenshot(...)

# Определяем координаты через Qwen3-VL
found_x, found_y = cloudflare_solver.find_checkbox(screenshot_path)

# Применяем фиксированную коррекцию
real_x = found_x + 130  # Коррекция -130px смещения
real_y = found_y + 25   # Коррекция -25px смещения

# С grid 50px для лучшей точности:
# real_x = found_x + 68
# real_y = found_y + 27
# Итоговая ошибка: ~73px
```

**Плюсы**:
- ✅ Стабильное смещение (-130px, -25px)
- ✅ С grid 50px → 73px ошибка (44.7% улучшение)
- ✅ Простая реализация
- ✅ Не зависит от текущего разрешения дисплея

**Минусы**:
- ⚠️ Требует изменения логики парсера
- ⚠️ Все равно остается ~73px ошибка даже с grid

### Вариант 2: Адаптивная коррекция по разрешению ⚠️

**Подход**: Определять разрешение скриншота и применять соответствующую коррекцию

```python
def get_correction(width, height):
    if width <= 800:
        return (-115, -110)  # 800×600
    elif width <= 1280:
        return (-12, -80)    # 1280×720
    else:
        return (130, 25)     # 1920×1080
```

**Плюсы**:
- ✅ Работает на любом разрешении

**Минусы**:
- ❌ Сложная реализация
- ❌ Требует калибровки для каждого разрешения
- ❌ Cloudflare может рендериться по-разному

### Вариант 3: Локализация виджета → поиск внутри 🔬

**Подход**:
1. Сначала найти весь Turnstile widget (200×65px)
2. Обрезать изображение до этой области
3. Внутри обрезанной области найти чекбокс

**Плюсы**:
- ✅ Теоретически самый точный (7-8px на обрезанном)
- ✅ Не зависит от разрешения

**Минусы**:
- ❌ Сложная реализация (два этапа)
- ❌ Автоматическая обрезка показала плохие результаты (92-105px)
- ❌ Требует точного определения границ виджета

### Вариант 4: Использование другой модели 🤖

**Подход**: Попробовать более крупные vision модели

Кандидаты:
- `qwen3-vl:32b` (21GB, более точная версия)
- GPT-4 Vision (платно, но может быть точнее)
- Claude 3.5 Sonnet с vision (платно)

**Плюсы**:
- ✅ Может быть значительно точнее

**Минусы**:
- ❌ Дорого (GPT-4V, Claude) или требует много VRAM (32b)
- ❌ Не факт что решит проблему

## Итоговая рекомендация

### 🎯 **Вариант 1: Фиксированное разрешение 1920×1080 + Grid 50px**

**Реализация:**

1. Изменить `czbooks_parser.py`:
```python
# Строка ~240: Установка размера окна
driver.set_window_size(1920, 1080)
```

2. В `cloudflare_solver_ollama.py`:
```python
def solve_turnstile(self, screenshot_path):
    # Создать grid version
    grid_path = self._add_grid(screenshot_path, grid_step=50)

    # Найти координаты
    found_x, found_y = self.find_checkbox(grid_path)

    # Применить коррекцию для 1920×1080 + grid
    real_x = found_x + 68
    real_y = found_y + 27

    return real_x, real_y
```

**Ожидаемая точность**: ~73px ошибка (приемлемо для Cloudflare Turnstile)

**Альтернатива без grid**:
- Коррекция: `real_x = found_x + 130`, `real_y = found_y + 25`
- Ожидаемая ошибка: ~132px

## Дополнительные тесты

Если точность 73px недостаточна, рекомендуется:

1. **Протестировать qwen3-vl:32b** (более крупная модель):
```bash
ollama pull qwen3-vl:32b  # Требует 21GB VRAM
```

2. **Попробовать множественные запросы с усреднением**:
```python
# 3 запроса, медиана координат
coords = [find_checkbox(screenshot) for _ in range(3)]
real_x = median([x for x, y in coords])
real_y = median([y for x, y in coords])
```

3. **Тестировать на реальных Cloudflare страницах**:
- Сохранять скриншоты при каждом срабатывании
- Собрать статистику успешных/неудачных кликов
- Калибровать коррекцию на реальных данных

## Приложение: Все тестовые скрипты

Созданные скрипты для анализа:
1. `test_stability_100_runs.py` - проверка детерминизма (100 запросов)
2. `test_offset_on_resized.py` - анализ смещения на resize
3. `test_with_grid.py` - тестирование grid подхода
4. `test_offset_on_real_resolutions.py` - реальные vs программные resize
5. `test_grid_on_real_resolutions.py` - grid на реальных разрешениях
6. `capture_like_parser.py` - захват скриншотов как парсер

## Статистика тестирования

- **Всего запросов к модели**: ~150+
- **Протестировано промптов**: 15+
- **Протестировано разрешений**: 10+
- **Протестировано grid размеров**: 6 (25px, 50px, 75px, 100px, 150px, 200px)
- **Общее время тестирования**: ~4 часа
